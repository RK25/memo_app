<!DOCTYPE html>
<html>

<head>
  <title><%= title %></title>
  <link rel='stylesheet' href='/stylesheets/style.css' />

</head>

<body>

  <head>
    <h1><%= title %></h1>
  </head>

  <div role="main">
    <input class="button_add" type="button" onclick="location.href='/new'" value="追加">
    <ul>
      <% for (var i = 0; i < content.length; i++) { %>
      <% if (content[i]  != ""){%>
      <li><a href="/show?id=<%= i+1 %>"><%= JSON.parse(content[i]).msg.split("\n")[0] %></a></li>
      <% } %>
      <% } %>
    </ul>
  </div>

</body>

</html>